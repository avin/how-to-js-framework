# Приложение A: Сравнение с React и Vue

## Введение

Теперь, когда вы создали свой собственный фреймворк, давайте сравним его с популярными решениями: React и Vue. Это поможет понять, что общего у всех фреймворков и какие подходы они используют для решения одних и тех же проблем.

## Общие принципы

### Что одинаково

Все современные фреймворки используют:

1. **Virtual DOM** (или похожую технику)
   - React: VDOM + Fiber
   - Vue: VDOM с реактивностью
   - NanoFramework: Простой VDOM

2. **Компонентная архитектура**
   - Переиспользуемые блоки
   - Изоляция состояния
   - Композиция

3. **Декларативный подход**
   - Описываете "что", а не "как"
   - Автоматическое обновление UI

4. **Однонаправленный поток данных**
   - Props вниз, события вверх
   - Предсказуемое состояние

## Создание элементов

### NanoFramework
```javascript
h('div', { class: 'container' },
  h('h1', null, 'Заголовок'),
  h('p', null, 'Текст')
)
```

### React
```javascript
// С JSX
<div className="container">
  <h1>Заголовок</h1>
  <p>Текст</p>
</div>

// Без JSX
React.createElement('div', { className: 'container' },
  React.createElement('h1', null, 'Заголовок'),
  React.createElement('p', null, 'Текст')
)
```

### Vue
```javascript
// SFC (Single File Component)
<template>
  <div class="container">
    <h1>Заголовок</h1>
    <p>Текст</p>
  </div>
</template>

// Render function
h('div', { class: 'container' }, [
  h('h1', 'Заголовок'),
  h('p', 'Текст')
])
```

**Вывод**: API почти идентичны! React.createElement и h() делают одно и то же.

## Компоненты

### NanoFramework
```javascript
function Counter({ initial = 0 }) {
  const [count, setCount] = useState(initial);

  return h('div', null,
    h('p', null, 'Счёт: ', count),
    h('button', { onclick: () => setCount(count + 1) }, '+')
  );
}
```

### React
```javascript
function Counter({ initial = 0 }) {
  const [count, setCount] = useState(initial);

  return (
    <div>
      <p>Счёт: {count}</p>
      <button onClick={() => setCount(count + 1)}>+</button>
    </div>
  );
}
```

### Vue (Composition API)
```javascript
import { ref } from 'vue';

export default {
  props: ['initial'],
  setup(props) {
    const count = ref(props.initial || 0);

    return {
      count,
      increment: () => count.value++
    };
  },
  template: `
    <div>
      <p>Счёт: {{ count }}</p>
      <button @click="increment">+</button>
    </div>
  `
}
```

**Различия**:
- React и NanoFramework: useState возвращает значение напрямую
- Vue: ref возвращает объект, значение в `.value`

## Состояние (State)

### NanoFramework
```javascript
const [count, setCount] = useState(0);
setCount(count + 1);
setCount(prev => prev + 1); // Функциональное обновление
```

### React
```javascript
const [count, setCount] = useState(0);
setCount(count + 1);
setCount(prev => prev + 1);
```

### Vue (Composition API)
```javascript
const count = ref(0);
count.value++; // Прямое изменение, автоматическая реактивность
```

**Ключевое различие**:
- React/NanoFramework: Иммутабельные обновления через setter
- Vue: Мутабельная реактивность через Proxy

## Эффекты (Side Effects)

### NanoFramework
```javascript
useEffect(() => {
  console.log('Монтирование или обновление');

  return () => {
    console.log('Размонтирование');
  };
}, [count]);
```

### React
```javascript
useEffect(() => {
  console.log('Монтирование или обновление');

  return () => {
    console.log('Размонтирование');
  };
}, [count]);
```

> В React и NanoFramework cleanup-функция вызывается не только при реальном размонтировании. Сначала она срабатывает перед повторным выполнением эффекта, когда меняются значения из массива зависимостей (`count` в примере). При размонтировании компонента cleanup запускается ещё раз, так что вывод «Размонтирование» появится и при обновлении, и при удалении компонента из DOM.

### Vue (Composition API)
```javascript
import { watchEffect, onMounted, onUnmounted } from 'vue';

onMounted(() => {
  console.log('Монтирование');
});

watchEffect(() => {
  console.log('count:', count.value);
});

onUnmounted(() => {
  console.log('Размонтирование');
});
```

**Различия**:
- React/NanoFramework: Один хук для всех эффектов
- Vue: Разные хуки для разных lifecycle моментов

## Контекст

### NanoFramework
```javascript
const ThemeContext = createContext('light');

function App() {
  return h(ThemeContext.Provider, { value: 'dark' },
    h(Button)
  );
}

function Button() {
  const theme = useContext(ThemeContext);
  return h('button', { class: theme }, 'Кнопка');
}
```

### React
```javascript
const ThemeContext = createContext('light');

function App() {
  return (
    <ThemeContext.Provider value="dark">
      <Button />
    </ThemeContext.Provider>
  );
}

function Button() {
  const theme = useContext(ThemeContext);
  return <button className={theme}>Кнопка</button>;
}
```

### Vue (Provide/Inject)
```javascript
// Родитель
export default {
  setup() {
    provide('theme', 'dark');
    return () => h(Button);
  }
}

// Ребёнок
export default {
  setup() {
    const theme = inject('theme');
    return () => h('button', { class: theme }, 'Кнопка');
  }
}
```

**Сходства**: Все используют паттерн Provider/Consumer

## Refs

### NanoFramework
```javascript
function Input() {
  const inputRef = useRef(null);

  useEffect(() => {
    inputRef.current.focus();
  }, []);

  return h('input', { ref: inputRef });
}
```

### React
```javascript
function Input() {
  const inputRef = useRef(null);

  useEffect(() => {
    inputRef.current.focus();
  }, []);

  return <input ref={inputRef} />;
}
```

### Vue (Composition API)
```javascript
import { ref, onMounted } from 'vue';

export default {
  setup() {
    const inputRef = ref(null);

    onMounted(() => {
      inputRef.value.focus();
    });

    return {
      inputRef
    };
  },
  template: '<input ref="inputRef">'
}
```

**Сходства**: API практически идентичен

## Производительность и оптимизация

### React

**Проблемы**:
- Весь компонент перерисовывается при изменении state
- Нужна мемоизация: `React.memo`, `useMemo`, `useCallback`

```javascript
const MemoizedChild = React.memo(Child);

function Parent() {
  const [count, setCount] = useState(0);

  const handleClick = useCallback(() => {
    console.log('Клик');
  }, []);

  return <MemoizedChild onClick={handleClick} />;
}
```

**NanoFramework**: Такая же проблема, можно добавить memo

### Vue

**Преимущество**:
- Fine-grained реактивность: обновляются только зависимые части
- Не нужна мемоизация компонентов
- Компилятор оптимизирует шаблоны

```javascript
const count = ref(0);
const doubled = computed(() => count.value * 2);
// doubled пересчитается только когда изменится count
```

### Solid.js (для сравнения)

Ещё более мелкозернистая реактивность:
```javascript
const [count, setCount] = createSignal(0);

// DOM создаётся один раз, обновляется только текст
<div>Count: {count()}</div>
```

## Virtual DOM: за и против

### React, NanoFramework

**Подход**: Virtual DOM + diffing

**Плюсы**:
- Простая ментальная модель
- Кроссплатформенность (React Native)
- Предсказуемость

**Минусы**:
- Оверхед на diffing
- Память для VDOM
- Нужна мемоизация

### Vue

**Подход**: VDOM + реактивность + компилятор

**Плюсы**:
- Компилятор знает, что может измениться
- Меньше лишних проверок
- Автоматическая оптимизация

**Минусы**:
- Сложнее внутреннее устройство
- Требуется шаг сборки

### Svelte

**Подход**: Нет VDOM, компиляция в императивный код

**Плюсы**:
- Максимальная производительность
- Маленький размер бандла
- Не нужен runtime

**Минусы**:
- Больше размер кода компонентов
- Нет runtime API для динамики

## Размер бандла

Минимальные приложения (Hello World):

| Фреймворк | Размер (минифицирован + gzip) |
|-----------|-------------------------------|
| NanoFramework | ~2 KB (наша реализация) |
| Preact | ~4 KB (минимальная версия React) |
| Vue 3 | ~16 KB |
| React 18 | ~40 KB (React + ReactDOM) |
| Svelte | ~2 KB (скомпилированный) |
| Solid | ~7 KB |

**Вывод**: Наш фреймворк конкурентоспособен по размеру!

## Экосистема

### React

**Плюсы**:
- Огромная экосистема
- Много библиотек и инструментов
- Большое сообщество

**Минусы**:
- Нет официального роутера/state manager
- Много способов решить одну задачу

### Vue

**Плюсы**:
- Официальные решения: Vue Router, Pinia
- Полная документация
- Постепенное внедрение

**Минусы**:
- Меньшее сообщество чем React
- Меньше вакансий

### NanoFramework

**Реальность**:
- Учебный проект
- Нет экосистемы
- Но понимание того, как всё работает!

## Что мы реализовали vs что нет

### Есть в NanoFramework

✅ Virtual DOM
✅ Компоненты (функциональные)
✅ Hooks (useState, useEffect, useContext, useRef)
✅ Diffing и patching
✅ Контекст
✅ Refs
✅ Базовая оптимизация (keys)

### Нет в NanoFramework

❌ JSX (используем h())
❌ Class компоненты
❌ Fragments (можно добавить)
❌ Suspense
❌ Concurrent Mode
❌ Server-Side Rendering
❌ Error Boundaries
❌ Portals
❌ useMemo, useCallback (можно добавить)
❌ Custom hooks (можно делать прямо сейчас!)
❌ DevTools
❌ TypeScript типы

## Сравнительная таблица

| Фича | NanoFramework | React | Vue | Svelte |
|------|---------------|-------|-----|--------|
| Virtual DOM | ✅ Простой | ✅ Fiber | ✅ Оптимизированный | ❌ Нет |
| Реактивность | useState | useState | Proxy/Ref | Компилируется |
| Hooks | ✅ Базовые | ✅ Полные | ✅ Composition API | ❌ Не нужны |
| JSX/Templates | h() | JSX | Templates | Templates |
| Размер | ~2 KB | ~40 KB | ~16 KB | ~2 KB |
| Кривая обучения | Низкая | Средняя | Низкая | Низкая |
| Производительность | Средняя | Средняя | Высокая | Очень высокая |
| TypeScript | ❌ | ✅ | ✅ | ✅ |
| SSR | ❌ | ✅ | ✅ | ✅ |
| Сообщество | - | Огромное | Большое | Растущее |

## Примеры идентичного кода

### Счётчик

**NanoFramework / React (почти идентично)**
```javascript
function Counter() {
  const [count, setCount] = useState(0);
  return h('button',
    { onclick: () => setCount(count + 1) },
    `Счёт: ${count}`
  );
}
```

**Vue**
```vue
<script setup>
import { ref } from 'vue';
const count = ref(0);
</script>

<template>
  <button @click="count++">Счёт: {{ count }}</button>
</template>
```

**Svelte**
```svelte
<script>
let count = 0;
</script>

<button on:click={() => count++}>
  Счёт: {count}
</button>
```

### Fetch данных

**NanoFramework / React**
```javascript
function Users() {
  const [users, setUsers] = useState([]);

  useEffect(() => {
    fetch('/api/users')
      .then(r => r.json())
      .then(setUsers);
  }, []);

  return h('ul', null,
    users.map(u => h('li', { key: u.id }, u.name))
  );
}
```

**Vue**
```vue
<script setup>
import { ref, onMounted } from 'vue';

const users = ref([]);

onMounted(async () => {
  users.value = await fetch('/api/users').then(r => r.json());
});
</script>

<template>
  <ul>
    <li v-for="u in users" :key="u.id">{{ u.name }}</li>
  </ul>
</template>
```

## Что мы узнали

1. **Все фреймворки очень похожи**
   - Одни и те же проблемы
   - Похожие решения
   - Разные компромиссы

2. **Virtual DOM не единственный путь**
   - Svelte: компиляция
   - Solid: fine-grained реактивность
   - Vue: комбинация подходов

3. **API почти стандартизировано**
   - h() / createElement
   - Hooks
   - Provider/Consumer

4. **Каждый фреймворк — это компромисс**
   - React: экосистема vs сложность
   - Vue: баланс vs vendor lock-in
   - Svelte: производительность vs размер компонентов

## Что выбрать для реальных проектов?

### React
**Когда**: Большая команда, много вакансий, нужна экосистема

### Vue
**Когда**: Хотите официальные решения из коробки, постепенное внедрение

### Svelte
**Когда**: Производительность критична, небольшой проект

### NanoFramework
**Когда**: Нужно понять, как всё работает! 😊

## Заключение

Создав собственный фреймворк, вы теперь понимаете:
- Как работает Virtual DOM
- Почему React использует hooks
- Зачем нужна мемоизация
- Как работает реактивность
- Что такое reconciliation

Это знание делает вас **лучшим разработчиком**, независимо от того, какой фреймворк вы используете!

---

[Следующее приложение: Дальнейшее развитие →](./b-future-improvements.md)
